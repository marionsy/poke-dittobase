<div class="col-4 mb-5 mt-5 align-self-start">
  <div class="card bg-primary" style="width: 25rem;">
    <img src={{image}}
      class="card-img-top" alt={{name}}>
    <div class="card-body">
      <h5 class="card-title">{{name}}</h5>
      <p class="card-text">{{description}}</p>
    </div>
    <ul class="list-group list-group-flush">
      <li class="list-group-item bg-info">Ability: {{ability}}</li>
      <li class="list-group-item bg-info">Original Game: {{game}}</li>
    </ul>
    <div class="card-body bg-danger">
      <a href="#" class="card-link delete-from-favorites" pokemon-name={{name}}>Delete from favorites</a>
    </div>
  </div>
</div>